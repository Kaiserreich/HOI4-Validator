from collections import defaultdict

# all global dicts and vars go here
PREPARSED_IDEAS_DICT = defaultdict(list)
PREPARSED_FOCUSES_DICT = defaultdict(list)
IDEAS_DICT = dict()
IDEA_SLOTS_DICT = defaultdict(list)
FOCUSES_DICT = dict()
FOCUS_TREES_DICT = defaultdict(list)